@model catalogo_jogos.Models.Jogo
@{
    ViewData["Title"] = "Home";
    int currentYear = DateTime.Now.Year;
}

<style>
    html, body {
        height: 100%;
        margin: 0;
        padding: 0;
        background-color: #121212;
        color: #e0e0e0;
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }

    .viewport-top {
        min-height: calc(100vh - 56px);
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
        padding-top: 40px;
    }

    .form-card-container {
        width: 100%;
        max-width: 520px;
        background: #1e1e1e;
        border: 1px solid #2d2d2d;
        border-radius: 12px;
        padding: 30px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    }

    .form-control, .form-select {
        background-color: #2a2a2a !important;
        border: 1px solid #3d3d3d !important;
        color: #fff !important;
        border-radius: 6px;
    }

        .form-control:focus, .form-select:focus {
            border-color: #0d6efd !important;
            box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25) !important;
        }

    label, h3 {
        color: #ffffff;
    }

    .btn-primary {
        background-color: #0d6efd;
        border: none;
        padding: 10px;
        font-weight: 600;
        border-radius: 6px;
        transition: all 0.2s ease;
    }

        .btn-primary:hover {
            background-color: #0b5ed7;
            transform: translateY(-1px);
        }
</style>

<div class="viewport-top">
    <div class="form-card-container">
        <h3 class="mb-4 text-center fw-bold">Catálogo de Jogos</h3>

        <form asp-action="SaveGame" method="post">

            <div class="mb-3 row align-items-center">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="Name">Título:</label>
                <div class="col-sm-7">
                    <input type="text" class="form-control" placeholder="Ex: Resident Evil" asp-for="Name" />
                </div>
            </div>

            <div class="mb-3 row align-items-center">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="Year">Ano Que Foi Jogado:</label>
                <div class="col-sm-7">
                    <select asp-for="Year" id="selectYear" class="form-select">
                        @for (int year = currentYear + 1; year >= 2002; year--)
                        {
                            if (year == currentYear)
                            {
                                <option value="@year" selected>@year</option>
                            }
                            else
                            {
                                <option value="@year">@year</option>
                            }
                        }
                    </select>
                </div>
            </div>

            <div class="mb-4 row align-items-center">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="Grade">Nota:</label>
                <div class="col-sm-7">
                    <select class="form-select" asp-for="Grade">
                        <option selected disabled value="">Selecione uma nota</option>
                        <option value="Obra-Prima">Obra-Prima</option>
                        <option value="Excelente">Excelente</option>
                        <option value="Muito Bom">Muito Bom</option>
                        <option value="Bom">Bom</option>
                        <option value="Mediano">Mediano</option>
                        <option value="Ruim">Ruim</option>
                        <option value="Péssimo">Péssimo</option>
                        <option value="Lixo Podre">Lixo Podre</option>
                    </select>
                </div>
            </div>

            <div class="mb-3 row align-items-center">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="FinishedInThisYear">Zerado Nesse Ano?:</label>
                <div class="col-sm-7">
                    <div class="form-check pt-1">
                        <input asp-for="FinishedInThisYear" type="checkbox" class="form-check-input" value="true" id="finishedCheckbox" style="transform: scale(1.2);" />
                    </div>
                </div>
            </div>

            <div class="mb-3 row align-items-center" id="lastFinishedContainer" style="display: none;">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="IsLastFinished">Este foi o último que zerei?</label>
                <div class="col-sm-7">
                    <div class="form-check pt-1">
                        <input asp-for="IsLastFinished" type="checkbox" class="form-check-input" value="true" id="lastFinishedCheckbox" style="transform: scale(1.2);" />
                    </div>
                </div>
            </div>

            <div class="mb-4 row align-items-center">
                <label class="col-sm-5 col-form-label fw-semibold" asp-for="Platinado">Platinado?:</label>
                <div class="col-sm-7">
                    <div class="form-check pt-1">
                        <input asp-for="Platinado" type="checkbox" class="form-check-input" value="true" style="transform: scale(1.2);" />
                    </div>
                </div>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary">Salvar Registro</button>
                <a asp-action="ListaJogos" class="btn btn-outline-light border-secondary">Ver Lista de Jogos</a>
            </div>

            @if (ViewBag.Mensagem != null)
            {
                <div class="alert alert-success mt-3 text-center border-0 bg-success bg-opacity-25 text-success-emphasis">@ViewBag.Mensagem</div>
            }
        </form>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var yearSelect = document.getElementById('selectYear');
            var lastFinishedContainer = document.getElementById('lastFinishedContainer');
            var currentYear = @currentYear;
            var finishedCheck = document.getElementById('finishedCheckbox');
            var lastFinishedCheck = document.getElementById('lastFinishedCheckbox');

            function toggleLastFinishedCheckbox() {
                if (parseInt(yearSelect.value) === currentYear) {
                    lastFinishedContainer.style.display = 'flex';
                } else {
                    lastFinishedContainer.style.display = 'none';
                    lastFinishedContainer.querySelector('input').checked = false;
                }
            }
            toggleLastFinishedCheckbox();
            yearSelect.addEventListener('change', toggleLastFinishedCheckbox);

            lastFinishedCheck.addEventListener('change', function() {
                if (lastFinishedCheck.checked) {
                    finishedCheck.checked = true;
                }
            });

            finishedCheck.addEventListener('change', function() {
                if (!finishedCheck.checked) {
                    lastFinishedCheck.checked = false;
                }
            });
        });
    </script>
}